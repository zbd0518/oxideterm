# OxideTerm Agent Guidelines

本文件适用于整个仓库。若子目录以后增加更具体的 `AGENTS.md`，则子目录规则优先。

## 基本原则

- 在完成用户给出的目标前持续推进；遇到阻塞时先穷尽安全、只读且在范围内的检查。
- 不默认接受用户对根因的判断。修改前先检查真实代码路径并确认根因。
- 采用能够正确解决问题的最小方案，不增加未要求的功能、抽象、兼容层或配置项。
- 只有已发布版本的公开行为或持久化格式，以及明确纳入支持范围的外部格式，才构成兼容合同。开发中尚未发布的字段、行为和格式应直接调整或删除，不得为其新增迁移逻辑、兼容分支或遗留兜底，除非用户明确要求。
- 只修改与当前目标直接相关的文件和代码行，不顺手重构、格式化或删除无关代码。
- 使用语义清晰的变量、类型和函数名，不硬编码可推导或已有常量表达的值。
- 若存在多种合理解释且选择会显著改变结果，先说明差异并请求确认。

## 实现要求

- 所有代码注释必须使用英语，并说明原因、边界或所有权，不复述代码表面行为。
- 前端和用户可见文案必须接入现有国际化机制，不得直接硬编码展示文本；新增或修改文案时必须同步维护项目现有 11 个语言包，并运行语言键集合一致性检查，不能只依赖英语回退。
- 用户界面改动必须优先复用项目已有的组件、交互状态、主题令牌和弹层机制；仅在现有组件确实无法满足需求时才自定义界面，且自定义方案的优先级不得高于复用方案。设计类技能只用于审视信息层级与布局，不能成为绕过现有组件体系的理由。
- 遵循现有代码风格、模块边界和所有权模型，不为单次使用创建通用框架。
- 新增长生命周期任务时，必须明确所有者、取消路径和清理时机。
- 不允许通过削弱测试、吞掉错误、增加测试专用分支或静默降级来获得通过结果。
- 修改产生的未使用导入、变量、函数或文件应一并清理；不要清理修改前已存在的无关问题。

## 修改前检查

开始编码前回答以下问题：

1. 用户描述的原因是否已由代码或可复现行为证明？
2. 真正的根因是什么？
3. 最小且正确的修改是什么？
4. 修改可能破坏哪些行为、数据、安全边界或性能路径？
5. 如何用可执行检查证明目标已经完成？

多步骤任务应先形成简短的可验证计划，例如：

```text
1. 复现问题 -> 验证：失败测试稳定出现
2. 实现最小修复 -> 验证：失败测试转为通过
3. 回归检查 -> 验证：相关测试和编译检查通过
```

## 测试与验证

- 修复缺陷时优先添加能够复现缺陷的聚焦测试，再实现修复。
- 只添加直接覆盖用户合同、真实风险或回归路径的测试，不为覆盖率堆砌测试。
- 每个测试和断言都必须验证真实可观察行为或明确风险边界；禁止添加恒真断言、重复断言、只绑定实现细节或没有回归价值的测试。
- 优先测试公开行为和高风险分支，避免依赖实现细节的脆弱模拟。
- 验证范围应与风险相称：先运行聚焦测试，再运行相关 crate 的检查和必要回归测试。
- 性能敏感修改必须记录修改前基线，并在修改后使用同一环境和同一基准复测。
- 不得把未运行、被忽略、依赖人工操作或因环境限制无法执行的检查描述为已通过。
- 完成前检查 `git diff` 和 `git status`，确认没有覆盖用户改动或混入无关文件。

## 安全与敏感数据

- 密码、令牌、私钥、口令、认证头和可能包含凭据的终端内容均按敏感数据处理。
- 不得仅因输入包含敏感值而拒绝用户明确选择的受支持用法；安全措施不得擅自改变用户要求的功能语义。
- 例如用户主动提供 `ssh://user:password@host` 时，若该 URI 形式属于当前实现范围，应接收并按连接用途消费；通过短生命周期零化、禁止日志、诊断和历史留存、默认不持久化及必要风险提示进行保护，不得以“可能泄露”为由替用户禁用。
- 敏感值不得进入日志、追踪、通知、错误文本、诊断包、任务名称、调试输出或人工智能上下文。
- 临时拥有的敏感字符串和字节优先使用零化容器，并限制复制、异步捕获和生命周期。
- 持久化敏感值只能使用仓库指定的系统钥匙串或加密存储；普通设置文件只保存安全引用或非敏感元数据。
- 启动外部进程时保持参数边界，不得将不可信输入静默交给 Shell，也不得在失败后自动降级到 Shell。

## 会话与后台任务所有权

- SSH 节点连接的生命周期属于连接注册表或 `NodeRouter`，不能由单个终端窗格隐式拥有。
- 关闭终端窗格只移除该消费者，不得断开仍被 SFTP、端口转发或其他消费者使用的共享节点。
- SFTP、端口转发、重连、健康检查和子进程任务必须有明确的运行时所有者和取消路径。
- 不得通过查找“第一个关联终端窗格”来代替节点或连接身份解析。
- 跳板子节点必须在传输需要期间保持父连接所有权。
- 用户界面通过运行时事件观察连接状态，不得通过终端窗格是否存在推断节点存活状态。

## Git 与工作区

- 工作区可能包含用户未提交的改动；保留并绕开无关改动。
- 未经明确授权，不提交、不推送、不创建标签或拉取请求。
- 不使用会覆盖工作区的破坏性命令，例如 `git reset --hard` 或 `git checkout --`。
- 需要暂存时只暂存已确认属于当前任务的路径，不使用 `git add .` 或 `git add -A`。

## 沟通要求

- 以事实、代码证据和验证结果为先，不把推测写成结论。
- 发现用户方案会损害安全、性能、架构、可维护性或类型安全时，应明确说明并提出更小的正确方案。
- 不为迎合用户而默认同意，也不为反对而反对；目标是得到正确、可验证且有用的结果。
- 中文说明中避免在一句话内无必要地混用中英文；确需保留的技术术语除外。

## void instruction-to-output leakage（IMPORTANT）

Distinguish instructions from deliverable content. Embody the requirements; never restate them unless explicitly requested.

## 不写不必要的测试